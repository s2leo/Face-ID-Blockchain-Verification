# =============================================================================
# HH Goa 2026 — Task #3 Pipeline Dependencies
# =============================================================================

# Core
requests==2.31.0
python-dotenv==1.0.0
# Python 3.14-compatible releases. Older pins do not provide wheels for 3.14.
Pillow>=11.3.0
numpy>=2.3.0
opencv-contrib-python>=4.12.0.88

# Terminal UI
rich==13.7.1
tabulate==0.9.0
web3>=6.0.0

# Google Cloud Vision (optional — only needed for --provider google_vision)
google-cloud-vision==3.7.2

# Gemini Vision (optional — only needed for Step 1.5, free tier)
# No SDK required — uses REST API directly via requests
# Get free key: https://aistudio.google.com → "Get API Key"
